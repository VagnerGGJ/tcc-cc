# FastAPI
fastapi==0.111.1
uvicorn[standard]==0.23.2

# Banco de dados
sqlalchemy==2.0.22
psycopg2-binary==2.9.7

# Segurança e autenticação
passlib[bcrypt]==1.7.4
python-jose==3.3.0

# Variáveis de ambiente
python-dotenv==1.0.0

# Validação de dados
pydantic==2.4.1

# Para chamadas HTTP externas (ex: integração IA)
requests==2.32.0

# Migrações de banco de dados (opcional)
alembic==1.12.0

# Testes
pytest==7.4.2
pytest-asyncio==0.21.0

# Utilitários
python-multipart==0.0.6  # Para upload de arquivos, se precisar no futuro
